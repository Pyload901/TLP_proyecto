start
  int x;
  y = ['a'];
  y = arr[1];
  for (int i = 0; i < 10; i = i + 1) start
    x = x + i;
  end
  while (x < 100) start
    x = x + 10;
  end
  if (x == 100) start
    x = 0;
  end else start
    x = 1;
  end
  exec hola(x,y);
  return 0;
end
int proc suma(int a, int b) start
  return a + b;
end